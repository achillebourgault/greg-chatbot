# OpenRouter
# Get a key: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Optional but recommended by OpenRouter
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=greg-chatbot

# Greg server behavior
# Maximum number of <search_web ... /> tool calls per user request (safety cap).
# Default is 5 if unset.
GREG_MAX_TOOL_CALLS=5

# Optional: conversation logs (JSONL per conversation)
# Default behavior may enable logs in dev; set explicitly to control.
GREG_ENABLE_LOGS=1
GREG_LOG_DIR=.greg-logs
